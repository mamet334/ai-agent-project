# Core Workspaces

This directory contains the main layout and grid parsers for the metadata-driven workspaces.
For the Engineer Workspace, this directory will host:
- `EngineerWorkspace.jsx` (Container)
- `MetadataGridRenderer.jsx` (Parser)

*Implementation is pending Step 4 approval.*
