# Change Log — 4 Juli 2026

## FOLDERSELECTOR — ENGINEER WORKSPACE

**File Dibuat:**
- `frontend/src/components/FolderSelector.jsx`

**File Diubah:**
- `frontend/src/components/workbench/ConversationEngine.jsx`
- `frontend/electron/preload.cjs`

**Perubahan:**
- FolderSelector: komponen pemilih folder dengan deteksi otomatis Electron vs Browser
- ConversationEngine: integrasi FolderSelector di workspace Engineer (hanya muncul di ws-engineer)
- preload.cjs: adapter `openFolderDialog()` untuk menjembatani handler `select-folder` di main.cjs

---

## TOMBOL COPY — CHAT BUBBLE

**File Diubah:**
- `frontend/src/components/workbench/ConversationEngine.jsx`

**Perubahan:**
- Tombol Copy muncul di pojok kanan atas setiap bubble AI (hover)
- Ikon berubah dari Copy → Check setelah diklik (reset 2 detik)
- Menggunakan `navigator.clipboard.writeText()`

---

## FIX: HTTP HEADER NON-ASCII

**File Diubah:**
- `frontend/src/components/workbench/ConversationEngine.jsx`

**Perubahan:**
- Membersihkan karakter non-ASCII dari token dan API key sebelum dikirim sebagai header HTTP
- `token.replace(/[^\x00-\x7F]/g, '')` mencegah error ISO-8859-1

---

## AGENT FORGE — FUNGSIONAL PENUH

**File Diubah:**
- `frontend/src/components/agent-forge/AgentForge.jsx`

**Perubahan:**
- Modal "Add Agent" dengan form nama, deskripsi, kategori, status
- Modal "Add Tool" dengan form nama, deskripsi, kategori
- Modal "Edit Agent" via ikon gear — bisa ubah status (active/inactive)
- Semua modal memanggil `AgentOrchestratorService.registerAgent()` dan `ToolRegistryService.registerTool()`
- EventBus listener untuk auto-refresh

---

## STATUS AKHIR

| Komponen | Status |
|----------|--------|
| FolderSelector | ✅ Production |
| Tombol Copy | ✅ Production |
| Agent Forge | ✅ Functional |
| HTTP Header Fix | ✅ Production |
| Engineer Workspace | ✅ Folder selector aktif |

# Change Log — 4 Juli 2026

## STORAGE MANAGER — MULTI-BACKEND

**File Diubah:**
- `frontend/src/core/runtime/StorageManager.js`

**Perubahan:**
- Tambah 3 backend: localStorage, file-system, memory
- Deteksi otomatis environment: Electron → file-system, Browser → localStorage
- Method setBackend(), getBackend(), listBackends()
- Backend file-system memanggil window.electronAPI untuk operasi baca/tulis

---

## FILE SYSTEM HANDLERS (ELECTRON)

**File Diubah:**
- `electron/main.cjs`
- `electron/preload.cjs`

**Perubahan:**
- Tambah 6 handler IPC: fs:readFile, fs:writeFile, fs:deleteFile, fs:listFiles, fs:getFileInfo, fs:fileExists
- preload.cjs mengekspos readFile, writeFile, deleteFile, listFiles, getFileInfo, fileExists
- main.cjs menangani semua operasi file system dengan path yang dinormalisasi

---

## FOLDER SELECTOR — ENGINEER WORKSPACE

**File Dibuat:**
- `frontend/src/components/FolderSelector.jsx`

**File Diubah:**
- `frontend/src/components/workbench/ConversationEngine.jsx`

**Perubahan:**
- FolderSelector: deteksi otomatis Electron vs Browser, tombol pilih folder, tampilkan path terpilih
- ConversationEngine: integrasi FolderSelector di workspace Engineer (hanya muncul di ws-engineer)

---

## TOMBOL COPY — CHAT BUBBLE

**File Diubah:**
- `frontend/src/components/workbench/ConversationEngine.jsx`

**Perubahan:**
- Tombol Copy di pojok kanan atas bubble AI (hover)
- Ikon Copy → Check setelah diklik, reset 2 detik

---

## FIX: HTTP HEADER NON-ASCII

**File Diubah:**
- `frontend/src/components/workbench/ConversationEngine.jsx`

**Perubahan:**
- Membersihkan karakter non-ASCII dari token dan API key dengan regex `[^\x00-\x7F]`

---

## AGENT FORGE — FUNGSIONAL PENUH

**File Diubah:**
- `frontend/src/components/agent-forge/AgentForge.jsx`

**Perubahan:**
- Modal Add Agent (nama, deskripsi, kategori, status)
- Modal Add Tool (nama, deskripsi, kategori)
- Modal Edit Agent via ikon gear (ubah status)
- Panggil AgentOrchestratorService.registerAgent() dan ToolRegistryService.registerTool()

---

## INTENT PARSER — FAREWELL

**File Diubah:**
- `frontend/src/core/runtime/services/IntentParser.js`

**Perubahan:**
- Tambah intent FAREWELL: dadah, sampai jumpa, bye, selamat tinggal
- Tambah method isFarewell()

---

## STATUS AKHIR

| Komponen | Status |
|----------|--------|
| StorageManager multi-backend | ✅ Production |
| File system handlers | ✅ Production |
| FolderSelector | ✅ Production |
| Tombol Copy | ✅ Production |
| Agent Forge | ✅ Functional |
| HTTP Header Fix | ✅ Production |
| IntentParser FAREWELL | ✅ Production |