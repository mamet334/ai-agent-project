// Grounding Parser (Stub for future extraction)
export function parseGroundingSources(sources: any[]): any[] {
  // Currently sources are passed as-is from plugin outputs, but we isolate the domain here for Wave 5.2G
  return sources || [];
}
